module.exports = () => {
    const helpText = `
    -help: to know all the supported command
    -v: to know the current version
    -t: generating template <options>
            vanillajs : generate boiler plate for vanilla Js
    `;

    console.log(helpText);
}